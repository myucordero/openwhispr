# Copy/Paste Launch Prompt for Codex

Use this after placing the package inside the OpenWhispr repository:

```text
Work in the current OpenWhispr repository and fully implement the WhisperX
accurate-recording and evidence-grounded reliable-notes pipeline.

First read every applicable existing repository instruction, including all
AGENTS.md and CLAUDE.md files. Then read
OpenWhispr-WhisperX-Codex-Agent-Package/CODEX_START_HERE.md and all normative
documents it references, plus the package's source references.

Treat the current repository as authoritative, preserve all unrelated changes,
record the baseline before editing, follow the tests-first phased plan, and
complete all code, tests, setup, packaging, benchmark tooling, documentation,
and validation that the environment permits. Do not stop at a plan or partial
prototype. Do not commit, push, release, or run a broad security scan unless I
explicitly authorize it.

Keep TASK_TRACKER.md updated while working. At the end, use
12_FINAL_REPORT_TEMPLATE.md exactly and distinguish PASS, FAIL, BLOCKED, and
unexecuted hardware/model checks with command evidence.
```

Adjust only the package path when it is copied under a different directory.
